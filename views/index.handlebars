<div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Bem vindo ao Brog</h1>
        <p class="col-md-8 fs-4">Este é um Brog teste feito usando Node e MongoDB</p>
       <a href="/usuarios/registro"> <button class="btn btn-primary btn-lg" type="button">Crie um conta</button></a>
      </div>
    </div>
    <hr>
<h2>Postagens recentes</h2>
{{#each postagens}}
    <div class="card mt-2">
        <div class="card-body">
            <a href="postagem/{{slug}}"><h3>{{titulo}}</h3></a>
            <p>{{descricao}}</p>
            <hr>
            <small>{{categoria.nome}}</small>
            <small>{{data}}</small>
        </div>
    </div>

{{else}}
    <h1>Nenhuma postagem recente encontrada &#128557</h1>
{{/each}}
<br>